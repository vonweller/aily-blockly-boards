name: Check and Deploy to NPM

on:
  push:
    branches: 
      - deploy

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
      has_publishable_packages: ${{ steps.set-matrix.outputs.has_publishable_packages }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changed packages
        id: set-matrix
        run: |
          # 获取最后一次成功运行的提交SHA或默认为一个空树
          ORIGIN_URL=$(git remote get-url origin)
          echo "Origin: $ORIGIN_URL"

          LAST_DEPLOY_REF=$(git ls-remote origin refs/tags/last-deploy | awk '{ print $1 }')
          if [ -n "$LAST_DEPLOY_REF" ]; then
            BASE_SHA="$LAST_DEPLOY_REF"
            echo "Using remote last-deploy tag as base: $BASE_SHA"
          else
            echo "No last-deploy tag found, using first commit in history"
            BASE_SHA=$(git rev-list --max-parents=0 HEAD)
          fi
          
          HEAD_SHA=${{ github.sha }}
          
          echo "Comparing $BASE_SHA...$HEAD_SHA"

          CHANGED_FILES=$(git diff --name-only --diff-filter=ACMRT "$BASE_SHA" "$HEAD_SHA")

          CHANGED_PACKAGES=()
          while IFS= read -r package_dir; do
            CHANGED_PACKAGES+=("$package_dir")
          done < <(
            printf '%s\n' "$CHANGED_FILES" |
            awk -F/ 'NF > 1 { print $1 }' |
            while read -r dir; do
              if [ -f "$dir/package.json" ] || printf '%s\n' "$CHANGED_FILES" | awk -v dir="$dir" 'index($0, dir "/") == 1 && /\.webp$/ { found=1 } END { exit !found }'; then
                printf '%s\n' "$dir"
              fi
            done |
            sort -u
          )

          if [ ${#CHANGED_PACKAGES[@]} -gt 0 ]; then
            echo "Detected changed packages: ${CHANGED_PACKAGES[*]}"
          fi

          HAS_PUBLISHABLE_PACKAGES=false
          for dir in "${CHANGED_PACKAGES[@]}"; do
            if [ -f "$dir/package.json" ]; then
              HAS_PUBLISHABLE_PACKAGES=true
              break
            fi
          done
          echo "has_publishable_packages=$HAS_PUBLISHABLE_PACKAGES" >> $GITHUB_OUTPUT
          
          if [ ${#CHANGED_PACKAGES[@]} -eq 0 ]; then
            echo "matrix={\"package\":[]}" >> $GITHUB_OUTPUT
            echo "No packages changed"
          else
            JSON_ARRAY=$(printf '"%s",' "${CHANGED_PACKAGES[@]}" | sed 's/,$//')
            echo "matrix={\"package\":[$JSON_ARRAY]}" >> $GITHUB_OUTPUT
            echo "Changed packages: ${CHANGED_PACKAGES[@]}"
          fi

  build-and-publish:
    name: Publish changed packages to CN npm registry
    needs: detect-changes
    if: ${{ github.event_name == 'push' && needs.detect-changes.outputs.matrix != '{"package":[]}' && github.ref == 'refs/heads/deploy' }}
    runs-on: [self-hosted, ailydeMac-mini, macOS, ARM64]
    timeout-minutes: 120
    env:
      NPM_CONFIG_FETCH_RETRIES: 5
      NPM_CONFIG_FETCH_RETRY_MINTIMEOUT: 10000
      NPM_CONFIG_FETCH_RETRY_MAXTIMEOUT: 120000
      NPM_CONFIG_FETCH_TIMEOUT: 300000
    strategy:
      matrix: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
      max-parallel: 1
      fail-fast: false
    steps:
      - name: Check UPDATE_ALL flag
        id: check-update-all
        run: |
          if [[ "${{ secrets.UPDATE_ALL }}" == "true" ]]; then
            echo "should_skip=true" >> $GITHUB_OUTPUT
          else
            echo "should_skip=false" >> $GITHUB_OUTPUT
          fi

      - name: echo matrix
        if: steps.check-update-all.outputs.should_skip != 'true'
        run: echo "${{ toJson(matrix) }}"

      - name: Checkout code
        if: steps.check-update-all.outputs.should_skip != 'true'
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GHB_TOKEN }}
      
      - name: Setup Node.js
        if: steps.check-update-all.outputs.should_skip != 'true'
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          registry-url: ${{ secrets.REGISTRY_URL }}
      
      # - name: Install dependencies
      #   run: |
      #     cd ${{ matrix.package }}
      #     npm ci || npm install
      
      - name: Check package.json exists
        id: check-pkg
        if: steps.check-update-all.outputs.should_skip != 'true'
        run: |
          if [ -f "${{ matrix.package }}/package.json" ]; then
            echo "has_package=true" >> $GITHUB_OUTPUT
          else
            echo "No package.json in ${{ matrix.package }}, skipping npm steps"
            echo "has_package=false" >> $GITHUB_OUTPUT
          fi

      - name: Build package
        if: steps.check-update-all.outputs.should_skip != 'true' && steps.check-pkg.outputs.has_package == 'true'
        run: |
          cd ${{ matrix.package }}
          npm run build --if-present

      - name: Test package
        if: steps.check-update-all.outputs.should_skip != 'true' && steps.check-pkg.outputs.has_package == 'true'
        run: |
          cd ${{ matrix.package }}
          npm test --if-present
      
      - name: Publish to CN npm registry
        if: steps.check-update-all.outputs.should_skip != 'true' && steps.check-pkg.outputs.has_package == 'true'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          retry_with_backoff() {
            local max_attempts="$1"
            local sleep_seconds="$2"
            shift 2
            local attempt=1

            until "$@"; do
              if [ "$attempt" -ge "$max_attempts" ]; then
                echo "Command failed after ${max_attempts} attempts: $*"
                return 1
              fi
              echo "Attempt ${attempt}/${max_attempts} failed. Retrying in ${sleep_seconds}s: $*"
              sleep "$sleep_seconds"
              attempt=$((attempt + 1))
              sleep_seconds=$((sleep_seconds * 2))
            done
          }

          cd ${{ matrix.package }}
          # 确保清理掉可能残留的 .npmrc
          rm -f .npmrc
          echo "Processing CN npm registry: ${{ secrets.REGISTRY_URL }}"
          
          # 获取当前版本号
          CURRENT_VERSION=$(node -p "require('./package.json').version")
          PACKAGE_NAME=$(node -p "require('./package.json').name")
          echo "Current version: $CURRENT_VERSION"
          
          # 检查当前版本是否已在国内 npm 仓库发布
          if npm view $PACKAGE_NAME@$CURRENT_VERSION version &> /dev/null; then
            echo "版本 $CURRENT_VERSION 已存在于国内 npm 仓库，尝试取消发布..."
            retry_with_backoff 3 3 npm unpublish $PACKAGE_NAME@$CURRENT_VERSION --force
            sleep 5
          fi
          
          # 发布
          retry_with_backoff 5 5 npm publish --access public

      - name: Notify CN npm registry cache bump
        if: steps.check-update-all.outputs.should_skip != 'true' && steps.check-pkg.outputs.has_package == 'true'
        env:
          NOTIFY_URL: ${{ secrets.NOTIFY_URL }}
          NOTIFY_TOKEN: ${{ secrets.NOTIFY_TOKEN }}
        run: |
          if [ -z "$NOTIFY_URL" ] || [ -z "$NOTIFY_TOKEN" ]; then
            echo "CN npm registry notify secrets not set, skipping."
            exit 0
          fi
          echo "Sending CN npm registry cache bump request to $NOTIFY_URL"
          set +e
          HTTP_STATUS=$(curl -s -o /tmp/notify_response.txt -w "%{http_code}" --max-time 30 \
            -X POST "$NOTIFY_URL" \
            -H "Authorization: Bearer $NOTIFY_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{}')
          CURL_EXIT=$?
          set -e
          if [ "$CURL_EXIT" -ne 0 ]; then
            echo "::warning::CN npm registry notification request failed (curl exit code $CURL_EXIT)"
          elif [ "$HTTP_STATUS" -ge 200 ] && [ "$HTTP_STATUS" -lt 300 ]; then
            echo "CN npm registry notification sent successfully (HTTP $HTTP_STATUS)"
          else
            echo "::warning::CN npm registry notification failed with HTTP status $HTTP_STATUS"
            cat /tmp/notify_response.txt || true
          fi

  publish-registry2:
    name: Publish changed packages to EU npm registry
    needs: detect-changes
    if: ${{ github.event_name == 'push' && needs.detect-changes.outputs.matrix != '{"package":[]}' && github.ref == 'refs/heads/deploy' }}
    runs-on: ubuntu-latest
    timeout-minutes: 120
    env:
      NPM_CONFIG_FETCH_RETRIES: 5
      NPM_CONFIG_FETCH_RETRY_MINTIMEOUT: 10000
      NPM_CONFIG_FETCH_RETRY_MAXTIMEOUT: 120000
      NPM_CONFIG_FETCH_TIMEOUT: 300000
    strategy:
      matrix: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
      fail-fast: false
    steps:
      - name: Check UPDATE_ALL flag
        id: check-update-all
        run: |
          if [[ "${{ secrets.UPDATE_ALL }}" == "true" ]]; then
            echo "should_skip=true" >> $GITHUB_OUTPUT
          else
            echo "should_skip=false" >> $GITHUB_OUTPUT
          fi

      - name: echo matrix
        if: steps.check-update-all.outputs.should_skip != 'true'
        run: echo "${{ toJson(matrix) }}"

      - name: Checkout code
        if: steps.check-update-all.outputs.should_skip != 'true'
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GHB_TOKEN }}

      - name: Setup Node.js
        if: steps.check-update-all.outputs.should_skip != 'true'
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Check package.json exists
        id: check-pkg
        if: steps.check-update-all.outputs.should_skip != 'true'
        run: |
          if [ -f "${{ matrix.package }}/package.json" ]; then
            echo "has_package=true" >> $GITHUB_OUTPUT
          else
            echo "No package.json in ${{ matrix.package }}, skipping npm steps"
            echo "has_package=false" >> $GITHUB_OUTPUT
          fi

      - name: Build package
        if: steps.check-update-all.outputs.should_skip != 'true' && steps.check-pkg.outputs.has_package == 'true'
        run: |
          cd ${{ matrix.package }}
          npm run build --if-present

      - name: Test package
        if: steps.check-update-all.outputs.should_skip != 'true' && steps.check-pkg.outputs.has_package == 'true'
        run: |
          cd ${{ matrix.package }}
          npm test --if-present

      - name: Publish to EU npm registry
        if: steps.check-update-all.outputs.should_skip != 'true' && steps.check-pkg.outputs.has_package == 'true'
        env:
          REGISTRY_2: ${{ secrets.REGISTRY_URL_2 }}
          TOKEN_2: ${{ secrets.NPM_TOKEN_2 }}
        run: |
          retry_with_backoff() {
            local max_attempts="$1"
            local sleep_seconds="$2"
            shift 2
            local attempt=1

            until "$@"; do
              if [ "$attempt" -ge "$max_attempts" ]; then
                echo "Command failed after ${max_attempts} attempts: $*"
                return 1
              fi
              echo "Attempt ${attempt}/${max_attempts} failed. Retrying in ${sleep_seconds}s: $*"
              sleep "$sleep_seconds"
              attempt=$((attempt + 1))
              sleep_seconds=$((sleep_seconds * 2))
            done
          }

          if [ -z "$REGISTRY_2" ] || [ -z "$TOKEN_2" ]; then
            echo "EU npm registry secrets not set."
            exit 1
          fi

          cd ${{ matrix.package }}
          trap 'rm -f .npmrc' EXIT

          CURRENT_VERSION=$(node -p "require('./package.json').version")
          PACKAGE_NAME=$(node -p "require('./package.json').name")
          echo "Current version: $CURRENT_VERSION"

          echo "----------------------------------------"
          echo "Processing EU npm registry: $REGISTRY_2"

          REGISTRY_2_NO_PROTO=$(echo "$REGISTRY_2" | sed 's/^https\?:\/\///')
          REGISTRY_2_NO_PROTO=$(echo "$REGISTRY_2_NO_PROTO" | sed 's/\/$//')

          echo "registry=$REGISTRY_2" > .npmrc
          echo "//$REGISTRY_2_NO_PROTO/:_authToken=$TOKEN_2" >> .npmrc

          if npm view $PACKAGE_NAME@$CURRENT_VERSION version &> /dev/null; then
             echo "Version $CURRENT_VERSION exists on EU npm registry, unpublishing..."
             retry_with_backoff 3 3 npm unpublish $PACKAGE_NAME@$CURRENT_VERSION --force
             sleep 5
          fi

          retry_with_backoff 5 5 npm publish --access public

      - name: Notify EU npm registry cache bump
        if: steps.check-update-all.outputs.should_skip != 'true' && steps.check-pkg.outputs.has_package == 'true'
        env:
          NOTIFY_URL: ${{ secrets.NOTIFY_URL_2 }}
          NOTIFY_TOKEN: ${{ secrets.NOTIFY_TOKEN_2 }}
        run: |
          if [ -z "$NOTIFY_URL" ] || [ -z "$NOTIFY_TOKEN" ]; then
            echo "EU npm registry notify secrets not set, skipping."
            exit 0
          fi
          echo "Sending EU npm registry cache bump request to $NOTIFY_URL"
          set +e
          HTTP_STATUS=$(curl -s -o /tmp/notify_response_2.txt -w "%{http_code}" --max-time 30 \
            -X POST "$NOTIFY_URL" \
            -H "Authorization: Bearer $NOTIFY_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{}')
          CURL_EXIT=$?
          set -e
          if [ "$CURL_EXIT" -ne 0 ]; then
            echo "::warning::EU npm registry notification request failed (curl exit code $CURL_EXIT)"
          elif [ "$HTTP_STATUS" -ge 200 ] && [ "$HTTP_STATUS" -lt 300 ]; then
            echo "EU npm registry notification sent successfully (HTTP $HTTP_STATUS)"
          else
            echo "::warning::EU npm registry notification failed with HTTP status $HTTP_STATUS"
            cat /tmp/notify_response_2.txt || true
          fi
  
  build-and-publish-all:
    name: Publish all packages to CN npm registry
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/deploy' }}
    runs-on: [self-hosted, ailydeMac-mini, macOS, ARM64]
    timeout-minutes: 240
    outputs:
      should_run: ${{ steps.check-update-all.outputs.should_run }}
    env:
      NPM_CONFIG_FETCH_RETRIES: 5
      NPM_CONFIG_FETCH_RETRY_MINTIMEOUT: 10000
      NPM_CONFIG_FETCH_RETRY_MAXTIMEOUT: 120000
      NPM_CONFIG_FETCH_TIMEOUT: 300000
    steps:
      - name: Check UPDATE_ALL flag
        id: check-update-all
        run: |
          if [[ "${{ secrets.UPDATE_ALL }}" == "true" ]]; then
            echo "should_run=true" >> $GITHUB_OUTPUT
          else
            echo "should_run=false" >> $GITHUB_OUTPUT
          fi
  
      - name: Checkout code
        if: steps.check-update-all.outputs.should_run == 'true'
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GHB_TOKEN }}
      
      - name: Setup Node.js
        if: steps.check-update-all.outputs.should_run == 'true'
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          registry-url: ${{ secrets.REGISTRY_URL }}
      
      - name: Publish all to CN npm registry
        if: steps.check-update-all.outputs.should_run == 'true'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_CONFIG_FETCH_RETRIES: 8
          NPM_CONFIG_FETCH_RETRY_MINTIMEOUT: 20000
          NPM_CONFIG_FETCH_RETRY_MAXTIMEOUT: 300000
          NPM_CONFIG_FETCH_TIMEOUT: 600000
        run: |
          retry_with_backoff() {
            local max_attempts="$1"
            local sleep_seconds="$2"
            shift 2
            local attempt=1

            until "$@"; do
              if [ "$attempt" -ge "$max_attempts" ]; then
                echo "Command failed after ${max_attempts} attempts: $*"
                return 1
              fi
              echo "Attempt ${attempt}/${max_attempts} failed. Retrying in ${sleep_seconds}s: $*"
              sleep "$sleep_seconds"
              attempt=$((attempt + 1))
              sleep_seconds=$((sleep_seconds * 2))
            done
          }

          run_with_timeout() {
            local timeout_seconds="$1"
            shift
            "$@" &
            local command_pid="$!"
            local elapsed=0

            while kill -0 "$command_pid" 2>/dev/null; do
              if [ "$elapsed" -ge "$timeout_seconds" ]; then
                echo "Command timed out after ${timeout_seconds}s: $*"
                kill "$command_pid" 2>/dev/null || true
                wait "$command_pid" 2>/dev/null || true
                return 124
              fi
              sleep 5
              elapsed=$((elapsed + 5))
            done

            wait "$command_pid"
          }

          retry_publish_with_timeout() {
            # 单次 publish 最多跑 20 分钟，避免网络卡死导致步骤长期阻塞。
            run_with_timeout 1200 npm publish --access public
          }

          # 确保清理掉可能残留的 .npmrc
          rm -f .npmrc
          echo "Processing CN npm registry: ${{ secrets.REGISTRY_URL }}"

          publish_default_one() {
            local dir="$1"
            (
              cd "$dir"
              echo "发布 $dir 到国内 npm 仓库"
              CURRENT_VERSION=$(node -p "require('./package.json').version")
              PACKAGE_NAME=$(node -p "require('./package.json').name")
              echo "当前版本: $CURRENT_VERSION"
              npm test --if-present

              if npm view $PACKAGE_NAME@$CURRENT_VERSION version &> /dev/null; then
                echo "版本 $CURRENT_VERSION 已存在于国内 npm 仓库，尝试取消发布..."
                retry_with_backoff 3 3 npm unpublish $PACKAGE_NAME@$CURRENT_VERSION --force
                sleep 5
              fi

              retry_with_backoff 8 10 retry_publish_with_timeout
            )
          }

          PACKAGE_DIRS=$(find . -name "package.json" -not -path "*/node_modules/*" -not -path "./package.json" -exec dirname {} \; | sed 's/^\.\///')
          echo "Found packages: $PACKAGE_DIRS"
          # 仅保留一级目录
          FIRST_LEVEL_DIRS=()
          for dir in $PACKAGE_DIRS; do
            if [[ "$dir" != *"/"* ]]; then
              FIRST_LEVEL_DIRS+=("$dir")
            fi
          done
          
          echo "要发布的一级目录包: ${FIRST_LEVEL_DIRS[*]}"
          
          for dir in "${FIRST_LEVEL_DIRS[@]}"; do
            if [ ! -d "$dir" ]; then
              echo "目录 $dir 不存在，跳过..."
              continue
            fi
            publish_default_one "$dir"
          done

      - name: Notify CN npm registry cache bump
        if: steps.check-update-all.outputs.should_run == 'true'
        env:
          NOTIFY_URL: ${{ secrets.NOTIFY_URL }}
          NOTIFY_TOKEN: ${{ secrets.NOTIFY_TOKEN }}
        run: |
          if [ -z "$NOTIFY_URL" ] || [ -z "$NOTIFY_TOKEN" ]; then
            echo "CN npm registry notify secrets not set, skipping."
            exit 0
          fi
          echo "Sending CN npm registry cache bump request to $NOTIFY_URL"
          set +e
          HTTP_STATUS=$(curl -s -o /tmp/notify_response.txt -w "%{http_code}" --max-time 30 \
            -X POST "$NOTIFY_URL" \
            -H "Authorization: Bearer $NOTIFY_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{}')
          CURL_EXIT=$?
          set -e
          if [ "$CURL_EXIT" -ne 0 ]; then
            echo "::warning::CN npm registry notification request failed (curl exit code $CURL_EXIT)"
          elif [ "$HTTP_STATUS" -ge 200 ] && [ "$HTTP_STATUS" -lt 300 ]; then
            echo "CN npm registry notification sent successfully (HTTP $HTTP_STATUS)"
          else
            echo "::warning::CN npm registry notification failed with HTTP status $HTTP_STATUS"
            cat /tmp/notify_response.txt || true
          fi

  publish-registry2-all:
    name: Publish all packages to EU npm registry
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/deploy' }}
    runs-on: ubuntu-latest
    timeout-minutes: 240
    outputs:
      should_run: ${{ steps.check-update-all.outputs.should_run }}
    env:
      NPM_CONFIG_FETCH_RETRIES: 5
      NPM_CONFIG_FETCH_RETRY_MINTIMEOUT: 10000
      NPM_CONFIG_FETCH_RETRY_MAXTIMEOUT: 120000
      NPM_CONFIG_FETCH_TIMEOUT: 300000
    steps:
      - name: Check UPDATE_ALL flag
        id: check-update-all
        run: |
          if [[ "${{ secrets.UPDATE_ALL }}" == "true" ]]; then
            echo "should_run=true" >> $GITHUB_OUTPUT
          else
            echo "should_run=false" >> $GITHUB_OUTPUT
          fi

      - name: Checkout code
        if: steps.check-update-all.outputs.should_run == 'true'
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GHB_TOKEN }}

      - name: Setup Node.js
        if: steps.check-update-all.outputs.should_run == 'true'
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Publish all to EU npm registry
        if: steps.check-update-all.outputs.should_run == 'true'
        env:
          REGISTRY_2: ${{ secrets.REGISTRY_URL_2 }}
          TOKEN_2: ${{ secrets.NPM_TOKEN_2 }}
        run: |
          retry_with_backoff() {
            local max_attempts="$1"
            local sleep_seconds="$2"
            shift 2
            local attempt=1

            until "$@"; do
              if [ "$attempt" -ge "$max_attempts" ]; then
                echo "Command failed after ${max_attempts} attempts: $*"
                return 1
              fi
              echo "Attempt ${attempt}/${max_attempts} failed. Retrying in ${sleep_seconds}s: $*"
              sleep "$sleep_seconds"
              attempt=$((attempt + 1))
              sleep_seconds=$((sleep_seconds * 2))
            done
          }

          if [ -z "$REGISTRY_2" ] || [ -z "$TOKEN_2" ]; then
            echo "EU npm registry secrets not set."
            exit 1
          fi

          trap 'rm -f .npmrc' EXIT

          REGISTRY_2_NO_PROTO=$(echo "$REGISTRY_2" | sed 's/^https\?:\/\///')
          REGISTRY_2_NO_PROTO=$(echo "$REGISTRY_2_NO_PROTO" | sed 's/\/$//')

          echo "registry=$REGISTRY_2" > .npmrc
          echo "//$REGISTRY_2_NO_PROTO/:_authToken=$TOKEN_2" >> .npmrc

          publish_registry2_one() {
            local dir="$1"
            (
              cd "$dir"
              echo "发布 $dir 到欧洲 npm 仓库"
              CURRENT_VERSION=$(node -p "require('./package.json').version")
              PACKAGE_NAME=$(node -p "require('./package.json').name")
              echo "当前版本: $CURRENT_VERSION"
              npm test --if-present

              if npm view $PACKAGE_NAME@$CURRENT_VERSION version &> /dev/null; then
                echo "Version $CURRENT_VERSION exists on EU npm registry, unpublishing..."
                retry_with_backoff 3 3 npm unpublish $PACKAGE_NAME@$CURRENT_VERSION --force
                sleep 5
              fi

              retry_with_backoff 5 5 npm publish --access public
            )
          }

          PACKAGE_DIRS=$(find . -name "package.json" -not -path "*/node_modules/*" -not -path "./package.json" -exec dirname {} \; | sed 's/^\.\///')
          echo "Found packages: $PACKAGE_DIRS"
          FIRST_LEVEL_DIRS=()
          for dir in $PACKAGE_DIRS; do
            if [[ "$dir" != *"/"* ]]; then
              FIRST_LEVEL_DIRS+=("$dir")
            fi
          done

          echo "要发布的一级目录包: ${FIRST_LEVEL_DIRS[*]}"

          for dir in "${FIRST_LEVEL_DIRS[@]}"; do
            if [ ! -d "$dir" ]; then
              echo "目录 $dir 不存在，跳过..."
              continue
            fi
            publish_registry2_one "$dir"
          done

      - name: Notify EU npm registry cache bump
        if: steps.check-update-all.outputs.should_run == 'true'
        env:
          NOTIFY_URL: ${{ secrets.NOTIFY_URL_2 }}
          NOTIFY_TOKEN: ${{ secrets.NOTIFY_TOKEN_2 }}
        run: |
          if [ -z "$NOTIFY_URL" ] || [ -z "$NOTIFY_TOKEN" ]; then
            echo "EU npm registry notify secrets not set, skipping."
            exit 0
          fi
          echo "Sending EU npm registry cache bump request to $NOTIFY_URL"
          set +e
          HTTP_STATUS=$(curl -s -o /tmp/notify_response_2.txt -w "%{http_code}" --max-time 30 \
            -X POST "$NOTIFY_URL" \
            -H "Authorization: Bearer $NOTIFY_TOKEN" \
            -H "Content-Type: application/json" \
            -d '{}')
          CURL_EXIT=$?
          set -e
          if [ "$CURL_EXIT" -ne 0 ]; then
            echo "::warning::EU npm registry notification request failed (curl exit code $CURL_EXIT)"
          elif [ "$HTTP_STATUS" -ge 200 ] && [ "$HTTP_STATUS" -lt 300 ]; then
            echo "EU npm registry notification sent successfully (HTTP $HTTP_STATUS)"
          else
            echo "::warning::EU npm registry notification failed with HTTP status $HTTP_STATUS"
            cat /tmp/notify_response_2.txt || true
          fi
  
  upload-changes-img:
    needs: detect-changes
    if: ${{ github.event_name == 'push' && needs.detect-changes.outputs.matrix != '{"package":[]}' && github.ref == 'refs/heads/deploy' }}
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{ fromJson(needs.detect-changes.outputs.matrix) }}
      fail-fast: false
    steps:
      - name: Check UPDATE_ALL_IMG flag
        id: check-update-all-img
        run: |
          if [[ "${{ secrets.UPDATE_ALL_IMG }}" == "true" ]]; then
            echo "should_skip=true" >> $GITHUB_OUTPUT
          else
            echo "should_skip=false" >> $GITHUB_OUTPUT
          fi

      - name: echo matrix
        if: steps.check-update-all-img.outputs.should_skip != 'true'
        run: echo "${{ toJson(matrix) }}"
      
      - name: Checkout code
        if: steps.check-update-all-img.outputs.should_skip != 'true'
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GHB_TOKEN }}
      
      - name: upload
        if: steps.check-update-all-img.outputs.should_skip != 'true'
        run: |
          cd ${{ matrix.package }}

          # 找到所有WebP图片文件
          echo "找到以下WebP图片文件："
          find . -type f -name "*.webp" -not -path "*/node_modules/*" -not -path "*/\.*/*"

          # 创建临时目录用于整理图片，保留父目录结构
          mkdir -p ../temp_images/${{ matrix.package }}

          # 遍历所有WebP图片并保持相对路径结构复制到临时目录
          find . -type f -name "*.webp" -not -path "*/node_modules/*" -not -path "*/\.*/*" | while read file; do
            # 保持相对路径结构
            relative_path="${file#./}"
            target_dir="../temp_images/${{ matrix.package }}/$(dirname "$relative_path")"
            mkdir -p "$target_dir"
            cp "$file" "$target_dir/"
            echo "复制 $file 到 $target_dir/$(basename "$file")"
          done

          # 检查是否有找到图片
          if [ -z "$(find ../temp_images/${{ matrix.package }} -name "*.webp" 2>/dev/null)" ]; then
            echo "没有找到任何WebP图片"
            exit 0
          fi

      - name: Determine R2 Destination
        id: r2_dest
        run: |
          if [ -z "${{ secrets.R2_IMG_TARGET }}" ]; then
            echo "path=${{ matrix.package }}" >> $GITHUB_OUTPUT
          else
            echo "path=${{ secrets.R2_IMG_TARGET }}/${{ matrix.package }}" >> $GITHUB_OUTPUT
          fi

      - name: Upload changed WebP images to RustFS
        if: steps.check-update-all-img.outputs.should_skip != 'true'
        env:
          RUSTFS_ENDPOINT: ${{ secrets.RUSTFS_ENDPOINT }}
          RUSTFS_ACCESS_KEY_ID: ${{ secrets.RUSTFS_ACCESS_KEY_ID }}
          RUSTFS_SECRET_ACCESS_KEY: ${{ secrets.RUSTFS_SECRET_ACCESS_KEY }}
          RUSTFS_BUCKET: ${{ secrets.RUSTFS_BUCKET }}
          RUSTFS_IMG_TARGET: ${{ secrets.RUSTFS_IMG_TARGET }}
        run: |
          if [ -z "$RUSTFS_ENDPOINT" ] || [ -z "$RUSTFS_ACCESS_KEY_ID" ] || [ -z "$RUSTFS_SECRET_ACCESS_KEY" ] || [ -z "$RUSTFS_BUCKET" ]; then
            echo "RustFS secrets not set, skipping."
            exit 0
          fi

          if [ ! -d "temp_images/${{ matrix.package }}" ] || [ -z "$(find "temp_images/${{ matrix.package }}" -type f -name "*.webp" -print -quit)" ]; then
            echo "No changed WebP images found, skipping RustFS upload."
            exit 0
          fi

          retry_with_backoff() {
            local max_attempts="$1"
            local sleep_seconds="$2"
            shift 2
            local attempt=1

            until "$@"; do
              if [ "$attempt" -ge "$max_attempts" ]; then
                echo "Command failed after ${max_attempts} attempts: $*"
                return 1
              fi
              echo "Attempt ${attempt}/${max_attempts} failed. Retrying in ${sleep_seconds}s: $*"
              sleep "$sleep_seconds"
              attempt=$((attempt + 1))
              sleep_seconds=$((sleep_seconds * 2))
            done
          }

          export AWS_ACCESS_KEY_ID="$RUSTFS_ACCESS_KEY_ID"
          export AWS_SECRET_ACCESS_KEY="$RUSTFS_SECRET_ACCESS_KEY"
          export AWS_DEFAULT_REGION="us-east-1"

          IMAGE_PREFIX="$RUSTFS_IMG_TARGET"
          IMAGE_PREFIX="${IMAGE_PREFIX#/}"
          IMAGE_PREFIX="${IMAGE_PREFIX%/}"
          if [ -n "$IMAGE_PREFIX" ]; then
            IMAGE_PREFIX="${IMAGE_PREFIX}/"
          fi

          while IFS= read -r file; do
            relative_path="${file#temp_images/}"
            target_key="${IMAGE_PREFIX}${relative_path}"
            echo "Uploading $file -> s3://${RUSTFS_BUCKET}/${target_key}"
            retry_with_backoff 3 3 aws --endpoint-url "$RUSTFS_ENDPOINT" s3 cp "$file" "s3://${RUSTFS_BUCKET}/${target_key}" --no-progress
          done < <(find "temp_images/${{ matrix.package }}" -type f -name "*.webp")

      - name: Upload to R2 (尝试 1/3)
        id: upload_r2_img_try_1
        if: steps.check-update-all-img.outputs.should_skip != 'true'
        continue-on-error: true
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --endpoint-url https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com --acl public-read --follow-symlinks
        env:
          AWS_S3_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_REGION: 'auto'
          SOURCE_DIR: 'temp_images/${{ matrix.package }}'
          DEST_DIR: '${{ steps.r2_dest.outputs.path }}'

      - name: 等待后重试R2上传 (尝试 2/3)
        if: steps.check-update-all-img.outputs.should_skip != 'true' && steps.upload_r2_img_try_1.outcome == 'failure'
        run: sleep 10

      - name: Upload to R2 (尝试 2/3)
        id: upload_r2_img_try_2
        if: steps.check-update-all-img.outputs.should_skip != 'true' && steps.upload_r2_img_try_1.outcome == 'failure'
        continue-on-error: true
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --endpoint-url https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com --acl public-read --follow-symlinks
        env:
          AWS_S3_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_REGION: 'auto'
          SOURCE_DIR: 'temp_images/${{ matrix.package }}'
          DEST_DIR: '${{ steps.r2_dest.outputs.path }}'

      - name: 等待后重试R2上传 (尝试 3/3)
        if: steps.check-update-all-img.outputs.should_skip != 'true' && steps.upload_r2_img_try_1.outcome == 'failure' && steps.upload_r2_img_try_2.outcome == 'failure'
        run: sleep 20

      - name: Upload to R2 (尝试 3/3)
        id: upload_r2_img_try_3
        if: steps.check-update-all-img.outputs.should_skip != 'true' && steps.upload_r2_img_try_1.outcome == 'failure' && steps.upload_r2_img_try_2.outcome == 'failure'
        continue-on-error: true
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --endpoint-url https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com --acl public-read --follow-symlinks
        env:
          AWS_S3_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_REGION: 'auto'
          SOURCE_DIR: 'temp_images/${{ matrix.package }}'
          DEST_DIR: '${{ steps.r2_dest.outputs.path }}'

      - name: R2上传失败并终止
        if: steps.check-update-all-img.outputs.should_skip != 'true' && steps.upload_r2_img_try_1.outcome == 'failure' && steps.upload_r2_img_try_2.outcome == 'failure' && steps.upload_r2_img_try_3.outcome == 'failure'
        run: |
          echo "R2 上传失败：3 次重试后仍未成功。"
          exit 1
  
  upload-all-img:
    runs-on: ubuntu-latest
    steps:
      - name: Check UPDATE_ALL_IMG flag
        id: check-update-all-img
        run: |
          if [[ "${{ secrets.UPDATE_ALL_IMG }}" == "true" ]]; then
            echo "should_run=true" >> $GITHUB_OUTPUT
          else
            echo "should_run=false" >> $GITHUB_OUTPUT
          fi

      - name: Checkout code
        if: steps.check-update-all-img.outputs.should_run == 'true'
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GHB_TOKEN }}
      
      - name: upload all img
        if: steps.check-update-all-img.outputs.should_run == 'true'
        id: find_images
        run: |
          echo "找到以下WebP图片文件："
          find . -type f -name "*.webp" -not -path "*/node_modules/*" -not -path "*/\.*/*"
          
          # 创建临时目录用于整理图片
          mkdir -p ./temp_images
          
          # 遍历所有WebP图片并保持相对路径结构复制到临时目录
          find . -type f -name "*.webp" -not -path "*/node_modules/*" -not -path "*/\.*/*" | while read file; do
            # 保持相对路径结构，包括父目录
            relative_path="${file#./}"
            target_dir="./temp_images/$(dirname "$relative_path")"
            mkdir -p "$target_dir"
            cp "$file" "$target_dir/"
            echo "复制 $file 到 $target_dir/$(basename "$file")"
          done

          # 检查是否有找到图片
          if [ -z "$(find ./temp_images -name "*.webp" 2>/dev/null)" ]; then
            echo "没有找到任何WebP图片"
            exit 0
          fi

      - name: Upload all WebP images to RustFS
        if: steps.check-update-all-img.outputs.should_run == 'true'
        env:
          RUSTFS_ENDPOINT: ${{ secrets.RUSTFS_ENDPOINT }}
          RUSTFS_ACCESS_KEY_ID: ${{ secrets.RUSTFS_ACCESS_KEY_ID }}
          RUSTFS_SECRET_ACCESS_KEY: ${{ secrets.RUSTFS_SECRET_ACCESS_KEY }}
          RUSTFS_BUCKET: ${{ secrets.RUSTFS_BUCKET }}
          RUSTFS_IMG_TARGET: ${{ secrets.RUSTFS_IMG_TARGET }}
        run: |
          if [ -z "$RUSTFS_ENDPOINT" ] || [ -z "$RUSTFS_ACCESS_KEY_ID" ] || [ -z "$RUSTFS_SECRET_ACCESS_KEY" ] || [ -z "$RUSTFS_BUCKET" ]; then
            echo "RustFS secrets not set, skipping."
            exit 0
          fi

          if [ ! -d "./temp_images" ] || [ -z "$(find ./temp_images -type f -name "*.webp" -print -quit)" ]; then
            echo "No WebP images found, skipping RustFS upload."
            exit 0
          fi

          retry_with_backoff() {
            local max_attempts="$1"
            local sleep_seconds="$2"
            shift 2
            local attempt=1

            until "$@"; do
              if [ "$attempt" -ge "$max_attempts" ]; then
                echo "Command failed after ${max_attempts} attempts: $*"
                return 1
              fi
              echo "Attempt ${attempt}/${max_attempts} failed. Retrying in ${sleep_seconds}s: $*"
              sleep "$sleep_seconds"
              attempt=$((attempt + 1))
              sleep_seconds=$((sleep_seconds * 2))
            done
          }

          export AWS_ACCESS_KEY_ID="$RUSTFS_ACCESS_KEY_ID"
          export AWS_SECRET_ACCESS_KEY="$RUSTFS_SECRET_ACCESS_KEY"
          export AWS_DEFAULT_REGION="us-east-1"

          IMAGE_PREFIX="$RUSTFS_IMG_TARGET"
          IMAGE_PREFIX="${IMAGE_PREFIX#/}"
          IMAGE_PREFIX="${IMAGE_PREFIX%/}"
          if [ -n "$IMAGE_PREFIX" ]; then
            IMAGE_PREFIX="${IMAGE_PREFIX}/"
          fi

          while IFS= read -r file; do
            relative_path="${file#./temp_images/}"
            target_key="${IMAGE_PREFIX}${relative_path}"
            echo "Uploading $file -> s3://${RUSTFS_BUCKET}/${target_key}"
            retry_with_backoff 3 3 aws --endpoint-url "$RUSTFS_ENDPOINT" s3 cp "$file" "s3://${RUSTFS_BUCKET}/${target_key}" --no-progress
          done < <(find ./temp_images -type f -name "*.webp")

      - name: Upload to R2 (尝试 1/3)
        id: upload_all_r2_img_try_1
        if: steps.check-update-all-img.outputs.should_run == 'true'
        continue-on-error: true
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --endpoint-url https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com --acl public-read --follow-symlinks
        env:
          AWS_S3_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_REGION: 'auto'
          SOURCE_DIR: 'temp_images'
          DEST_DIR: '${{ secrets.R2_IMG_TARGET }}'

      - name: 等待后重试R2上传 (尝试 2/3)
        if: steps.check-update-all-img.outputs.should_run == 'true' && steps.upload_all_r2_img_try_1.outcome == 'failure'
        run: sleep 10

      - name: Upload to R2 (尝试 2/3)
        id: upload_all_r2_img_try_2
        if: steps.check-update-all-img.outputs.should_run == 'true' && steps.upload_all_r2_img_try_1.outcome == 'failure'
        continue-on-error: true
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --endpoint-url https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com --acl public-read --follow-symlinks
        env:
          AWS_S3_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_REGION: 'auto'
          SOURCE_DIR: 'temp_images'
          DEST_DIR: '${{ secrets.R2_IMG_TARGET }}'

      - name: 等待后重试R2上传 (尝试 3/3)
        if: steps.check-update-all-img.outputs.should_run == 'true' && steps.upload_all_r2_img_try_1.outcome == 'failure' && steps.upload_all_r2_img_try_2.outcome == 'failure'
        run: sleep 20

      - name: Upload to R2 (尝试 3/3)
        id: upload_all_r2_img_try_3
        if: steps.check-update-all-img.outputs.should_run == 'true' && steps.upload_all_r2_img_try_1.outcome == 'failure' && steps.upload_all_r2_img_try_2.outcome == 'failure'
        continue-on-error: true
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --endpoint-url https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com --acl public-read --follow-symlinks
        env:
          AWS_S3_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_REGION: 'auto'
          SOURCE_DIR: 'temp_images'
          DEST_DIR: '${{ secrets.R2_IMG_TARGET }}'

      - name: R2上传失败并终止
        if: steps.check-update-all-img.outputs.should_run == 'true' && steps.upload_all_r2_img_try_1.outcome == 'failure' && steps.upload_all_r2_img_try_2.outcome == 'failure' && steps.upload_all_r2_img_try_3.outcome == 'failure'
        run: |
          echo "R2 上传失败：3 次重试后仍未成功。"
          exit 1

  genJson-and-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GHB_TOKEN }}
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          registry-url: ${{ secrets.REGISTRY_URL }}
      
      - name: Generate JSON
        run: node .scripts/genjson.js
      - name: Generate AI JSON
        run: node .scripts/genjson-ai.js
      
      - name: Generate info.json for all boards
        run: node .scripts/generate-info.js --all
      
      - name: Generate boards-index.json
        run: node .scripts/generate-boards-index.js
      
      - name: Check if JSON file exists
        run: |
          if [ -f "./boards.json" ]; then
            echo "boards.json exists"
            cat ./boards.json | head -n 10
          else
            echo "boards.json does not exist"
            ls -la
            exit 1
          fi
        
      - name: Check if boards-index.json file exists
        run: |
          if [ -f "./boards-index.json" ]; then
            echo "boards-index.json exists"
            cat ./boards-index.json | head -n 10
          else
            echo "boards-index.json does not exist"
            ls -la
            exit 1
          fi

      - name: Check if boards-ai.json file exists
        run: |
          if [ -f "./boards-ai.json" ]; then
            echo "boards-ai.json exists"
            cat ./boards-ai.json | head -n 10
          else
            echo "boards-ai.json does not exist"
            ls -la
            exit 1
          fi

      - name: Upload boards JSON files to RustFS
        env:
          RUSTFS_ENDPOINT: ${{ secrets.RUSTFS_ENDPOINT }}
          RUSTFS_ACCESS_KEY_ID: ${{ secrets.RUSTFS_ACCESS_KEY_ID }}
          RUSTFS_SECRET_ACCESS_KEY: ${{ secrets.RUSTFS_SECRET_ACCESS_KEY }}
          RUSTFS_BUCKET: ${{ secrets.RUSTFS_BUCKET }}
          RUSTFS_TARGET: ${{ secrets.RUSTFS_TARGET }}
        run: |
          if [ -z "$RUSTFS_ENDPOINT" ] || [ -z "$RUSTFS_ACCESS_KEY_ID" ] || [ -z "$RUSTFS_SECRET_ACCESS_KEY" ] || [ -z "$RUSTFS_BUCKET" ]; then
            echo "RustFS secrets not set, skipping."
            exit 0
          fi

          retry_with_backoff() {
            local max_attempts="$1"
            local sleep_seconds="$2"
            shift 2
            local attempt=1

            until "$@"; do
              if [ "$attempt" -ge "$max_attempts" ]; then
                echo "Command failed after ${max_attempts} attempts: $*"
                return 1
              fi
              echo "Attempt ${attempt}/${max_attempts} failed. Retrying in ${sleep_seconds}s: $*"
              sleep "$sleep_seconds"
              attempt=$((attempt + 1))
              sleep_seconds=$((sleep_seconds * 2))
            done
          }

          export AWS_ACCESS_KEY_ID="$RUSTFS_ACCESS_KEY_ID"
          export AWS_SECRET_ACCESS_KEY="$RUSTFS_SECRET_ACCESS_KEY"
          export AWS_DEFAULT_REGION="us-east-1"

          PREFIX="$RUSTFS_TARGET"
          PREFIX="${PREFIX#/}"
          PREFIX="${PREFIX%/}"
          if [ -n "$PREFIX" ]; then
            PREFIX="${PREFIX}/"
          fi

          for file in boards.json boards-index.json boards-ai.json; do
            retry_with_backoff 3 3 aws --endpoint-url "$RUSTFS_ENDPOINT" s3 cp "$file" "s3://${RUSTFS_BUCKET}/${PREFIX}${file}" --no-progress
          done

      - name: Prepare R2 Upload
        run: |
          mkdir -p r2_json
          cp boards.json r2_json/
          cp boards-index.json r2_json/
          cp boards-ai.json r2_json/

      - name: Upload to R2 (尝试 1/3)
        id: upload_r2_json_try_1
        continue-on-error: true
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --endpoint-url https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com --acl public-read --follow-symlinks --cache-control no-store,no-cache,must-revalidate,max-age=0
        env:
          AWS_S3_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_REGION: 'auto'
          SOURCE_DIR: 'r2_json'
          DEST_DIR: '${{ secrets.R2_JSON_TARGET }}'

      - name: 等待后重试R2上传 (尝试 2/3)
        if: steps.upload_r2_json_try_1.outcome == 'failure'
        run: sleep 10

      - name: Upload to R2 (尝试 2/3)
        id: upload_r2_json_try_2
        if: steps.upload_r2_json_try_1.outcome == 'failure'
        continue-on-error: true
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --endpoint-url https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com --acl public-read --follow-symlinks --cache-control no-store,no-cache,must-revalidate,max-age=0
        env:
          AWS_S3_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_REGION: 'auto'
          SOURCE_DIR: 'r2_json'
          DEST_DIR: '${{ secrets.R2_JSON_TARGET }}'

      - name: 等待后重试R2上传 (尝试 3/3)
        if: steps.upload_r2_json_try_1.outcome == 'failure' && steps.upload_r2_json_try_2.outcome == 'failure'
        run: sleep 20

      - name: Upload to R2 (尝试 3/3)
        id: upload_r2_json_try_3
        if: steps.upload_r2_json_try_1.outcome == 'failure' && steps.upload_r2_json_try_2.outcome == 'failure'
        continue-on-error: true
        uses: jakejarvis/s3-sync-action@master
        with:
          args: --endpoint-url https://${{ secrets.R2_ACCOUNT_ID }}.r2.cloudflarestorage.com --acl public-read --follow-symlinks --cache-control no-store,no-cache,must-revalidate,max-age=0
        env:
          AWS_S3_BUCKET: ${{ secrets.R2_BUCKET }}
          AWS_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          AWS_REGION: 'auto'
          SOURCE_DIR: 'r2_json'
          DEST_DIR: '${{ secrets.R2_JSON_TARGET }}'

      - name: R2上传失败并终止
        if: steps.upload_r2_json_try_1.outcome == 'failure' && steps.upload_r2_json_try_2.outcome == 'failure' && steps.upload_r2_json_try_3.outcome == 'failure'
        run: |
          echo "R2 上传失败：3 次重试后仍未成功。"
          exit 1

  update-last-deploy-tag:
    runs-on: ubuntu-latest
    needs:
      - detect-changes
      - build-and-publish
      - publish-registry2
      - build-and-publish-all
      - publish-registry2-all
    if: >-
      ${{
        always() &&
        needs.detect-changes.result == 'success' &&
        (
          (
            needs.build-and-publish-all.outputs.should_run != 'true' &&
            needs.publish-registry2-all.outputs.should_run != 'true' &&
            needs.detect-changes.outputs.has_publishable_packages == 'true' &&
            needs.build-and-publish.result == 'success' &&
            needs.publish-registry2.result == 'success'
          ) ||
          (
            needs.build-and-publish-all.outputs.should_run == 'true' &&
            needs.publish-registry2-all.outputs.should_run == 'true' &&
            needs.build-and-publish-all.result == 'success' &&
            needs.publish-registry2-all.result == 'success'
          )
        )
      }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GHB_TOKEN }}

      - name: Update last deploy tag
        run: |
          git tag -f last-deploy ${{ github.sha }}
          git push -f origin last-deploy
