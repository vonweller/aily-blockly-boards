# 功能引脚申明规范

## 1. 适用范围

本规范用于在开发板的 `board.json` 中声明板载设备配置、固定功能引脚和功能可用引脚列表，例如显示屏、音频编解码器、摄像头、SD 卡、舵机和 WS2812 彩灯。

这些字段描述开发板与特定功能之间的连接或约束，不替代通用的 `digitalPins`、`pwmPins`、`spiPins`、`i2cPins` 等能力声明。

## 2. 顶层字段分类与命名

字段名统一采用 lowerCamelCase（小驼峰）形式，并根据承载的数据选择以下三种命名。

### 2.1 `xxxConfig`：设备或功能配置

同时包含设备元数据、通信参数和引脚信息时，使用 `<功能名>Config`：

```text
<functionName>Config
```

| 功能 | 标准字段名 | 说明 |
| --- | --- | --- |
| 显示设备 | `displayConfig` | 显示控制器、接口、尺寸、频率和引脚 |
| 音频设备 | `audioConfig` | 音频编解码器及其控制、数据引脚 |
| 摄像头 | `cameraConfig` | 摄像头接口、支持型号和数据引脚 |
| 舵机 | `servoConfig` | 可用舵机引脚及后续可扩展的舵机参数 |

应使用 `displayConfig`，不使用 `screenConfig`。`display` 是驱动和嵌入式配置中更通用的术语。

### 2.2 `xxxPins`：纯引脚集合

字段只包含一组引脚映射或可选引脚，并且没有设备型号、尺寸、频率等元数据时，可以使用 `<功能名>Pins`：

```json
{
    "sdCardPins": {
        "miso": "35",
        "sclk": "3",
        "mosi": "14"
    }
}
```

`servoPins` 是已经存在的舵机引脚列表方案，必须继续支持：

```json
{
    "servoPins": [
        ["GPIO2", "2"],
        ["GPIO4", "4"],
        ["GPIO5", "5"]
    ]
}
```

`servoPins` 适合只声明可用引脚的开发板；`servoConfig` 是可同时承载引脚和舵机参数的新方案。两者不是互斥字段。

### 2.3 `xxxPin`：单个功能引脚

功能只有一个信号引脚时，使用单数形式 `<功能名>Pin`：

```json
{
    "ws2812Pin": "48"
}
```

不要将单引脚字段命名为 `ws2812Pins`，也不要为了形式统一增加无意义的嵌套对象。

## 3. `xxxConfig` 通用结构

`xxxConfig` 必须是 JSON 对象。设备属性直接放在配置对象中，所有引脚统一放入 `pins`：

```json
{
    "functionConfig": {
        "property": "value",
        "pins": {
            "signal": "pinValue"
        }
    }
}
```

规则如下：

1. `pins` 之外的字段用于描述设备型号、接口类型或工作参数。
2. 固定信号映射使用 `pins` 对象。
3. 可选引脚列表使用 `pins` 二维数组。
4. 引脚编号统一使用字符串，以兼容 `A0`、`PA5`、`GPIO21` 等标识。
5. 尺寸、频率等数值配置使用 JSON 数字，不使用数字字符串。
6. 芯片、控制器、接口和型号名称建议使用小写形式。

## 4. 标准设备配置

### 4.1 `displayConfig`

显示设备使用以下结构：

```json
{
    "displayConfig": {
        "controller": "st7789",
        "interface": "spi",
        "width": 240,
        "height": 240,
        "frequency": 40000000,
        "pins": {
            "sclk": "21",
            "mosi": "47",
            "dc": "43",
            "cs": "44"
        }
    }
}
```

| 字段 | 类型 | 必需 | 说明 |
| --- | --- | --- | --- |
| `controller` | string | 是 | 显示控制器型号，例如 `st7789`、`ili9341` |
| `interface` | string | 是 | 通信接口，例如 `spi`、`qspi`、`i2c`、`parallel` |
| `width` | number | 是 | 显示宽度，单位为像素 |
| `height` | number | 是 | 显示高度，单位为像素 |
| `frequency` | number | 否 | 总线频率，单位为 Hz |
| `pins` | object | 是 | 显示设备的固定信号引脚 |

SPI 显示信号优先使用 `sclk`、`mosi`、`miso`、`cs`、`dc`、`rst`、`bl`。不要使用容易与 I²C 混淆的 `scl`、`sda` 表示 SPI 时钟和数据。

### 4.2 `audioConfig`

音频设备使用以下结构：

```json
{
    "audioConfig": {
        "codec": "es8311",
        "pins": {
            "mclk": "46",
            "din": "38",
            "lrck": "2",
            "dout": "40",
            "sda": "41",
            "scl": "42",
            "sclk": "39"
        }
    }
}
```

`codec` 表示音频编解码器型号。音频数据信号和用于寄存器配置的 I²C 信号均放在 `pins` 中。

### 4.3 `cameraConfig`

摄像头接口使用以下结构：

```json
{
    "cameraConfig": {
        "interface": "dvp",
        "supportedModels": [
            "ov2640",
            "gc2145"
        ],
        "pins": {
            "sda": "4",
            "scl": "5",
            "vsync": "6",
            "href": "7",
            "xclk": "15",
            "y9": "16",
            "y8": "17",
            "y7": "18",
            "y4": "8",
            "y3": "9",
            "y5": "10",
            "y2": "11",
            "y6": "12",
            "pclk": "13"
        }
    }
}
```

`supportedModels` 是接口已验证或明确支持的摄像头型号列表。摄像头同步和时钟信号使用 `vsync`、`href`、`xclk`、`pclk` 等明确名称。

### 4.4 `servoConfig`

`servoConfig` 是新的舵机配置方案。配置中的 `pins` 是可选引脚二维数组，每一项均为 `[显示名称, 引脚值]`：

```json
{
    "servoConfig": {
        "pins": [
            ["GPIO2", "2"],
            ["GPIO4", "4"],
            ["GPIO5", "5"]
        ]
    }
}
```

- 第一项是界面中显示的引脚名称。
- 第二项是代码生成或配置读取时使用的引脚值。
- 两项均必须使用字符串。
- 即使只有一个可用引脚，也必须保留二维数组结构。
- 仅列出开发板实际支持舵机输出的引脚，不应直接复制全部 `digitalPins`。

后续如果需要声明统一的舵机频率、脉宽范围等参数，可以直接扩展 `servoConfig`，无需再次改变顶层字段结构。

为兼容现有使用方，开发板可以同时提供 `servoPins` 和 `servoConfig`：

```json
{
    "servoPins": [
        ["GPIO2", "2"],
        ["GPIO4", "4"]
    ],
    "servoConfig": {
        "pins": [
            ["GPIO2", "2"],
            ["GPIO4", "4"]
        ]
    }
}
```

同时声明时，`servoPins` 与 `servoConfig.pins` 必须保持完全一致。重复保存是兼容过渡所需，不能让两个字段分别表达不同的引脚能力。

## 5. 引脚字段命名

信号字段应优先采用芯片数据手册、原理图和常用驱动中的标准缩写，并统一使用小写或 lowerCamelCase。

| 功能 | 推荐信号字段 |
| --- | --- |
| SPI 显示屏 | `sclk`、`mosi`、`miso`、`cs`、`dc`、`rst`、`bl` |
| 音频设备 | `mclk`、`din`、`lrck`、`dout`、`sda`、`scl`、`sclk` |
| SD 卡 | `miso`、`sclk`、`mosi`，按需增加 `cs` |
| DVP 摄像头 | `sda`、`scl`、`vsync`、`href`、`xclk`、`y2`～`y9`、`pclk` |

新增信号字段时应遵循以下规则：

1. 不使用中文拼音。
2. 字段名不包含 `GPIO`、`PIN` 或具体引脚编号。
3. 同一字段只对应一个固定信号，不使用斜杠拼接多个名称。
4. 同类设备已有字段可用时，不创建同义字段。

## 6. 读取与兼容规则

1. `xxxConfig` 不存在表示开发板未提供该设备的固定配置，库必须保留手动配置方式。
2. 配置对象部分字段缺失时，库可以对缺失字段使用自身默认值，但不能覆盖已经明确声明的字段。
3. 使用方应忽略无法识别的可选字段，以便规范后续扩展。
4. 通用总线能力仍从 `spiPins`、`i2cPins` 等字段读取，不能被设备配置替代。
5. 不应仅通过 `digitalPins` 中的显示名称推断板载设备连接，应优先读取对应的 `xxxConfig`、`xxxPins` 或 `xxxPin`。
6. 舵机库按 `servoConfig.pins` → `servoPins` → `digitalPins` 的顺序读取。字段不存在、为空或格式无效时继续向后回退，保证新旧开发板均可正常使用。
7. TFT 类库仅在 `displayConfig.controller` 和 `displayConfig.interface` 受支持时自动采用配置，否则回退到库原有的手动初始化方式。
8. `servoConfig.pins` 与 `servoPins` 同时存在但内容不一致时，以 `servoConfig.pins` 为准；使用方应输出警告，开发板配置维护者必须修正两者。

旧版字段 `displayPins`、`audioPins`、`cameraPins` 不应再用于新开发板。需要兼容已发布配置时，使用方可以在过渡期优先读取新字段，并将旧字段作为回退来源。`servoPins` 仍是受支持的兼容方案，不属于本条停止新增的旧版设备字段。

## 7. 检查清单

提交功能配置前，应确认：

- 顶层字段使用 lowerCamelCase。
- 包含元数据和引脚时使用 `xxxConfig`，引脚统一放入 `pins`。
- 纯引脚集合使用 `xxxPins`，单个引脚使用 `xxxPin`。
- `servoConfig.pins` 使用 `[显示名称, 引脚值]` 二维数组。
- 同时提供 `servoConfig.pins` 与 `servoPins` 时，两者内容完全一致。
- 所有引脚编号均为字符串，数值配置使用 JSON 数字。
- 信号名与数据手册或原理图一致。
- 同类设备没有使用含义相同但拼写不同的字段。
- `board.json` 是合法 JSON，且没有重复键。
